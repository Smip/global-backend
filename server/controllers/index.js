const user = require('./user');
const article = require('./article');
const article_content = require('./article-content');
const file = require('./file');
const tarifs = require('./tarifs');

module.exports = {
    user,
    article,
    article_content,
    file,
    tarifs
};
